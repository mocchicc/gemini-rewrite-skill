# 検討状況（架空のテスト用原稿）

導入候補は12校で、費用上限は500万円になっています。提供開始日はまだ決まっていません。先生が使いやすいようにするのが大事だと思っていて、そこを優先して改善をしていきたいと考えています。ただし、全校への導入を確約するものではありません。

技術メモは https://example.com/spec にあります。環境変数名は `FEATURE_ENABLED` です。

```python
FEATURE_ENABLED = False
```
